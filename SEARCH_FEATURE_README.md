# 美食地圖搜尋功能說明

## 功能概述

美食地圖的搜尋功能允許用戶通過兩個主要條件來搜尋餐廳：
1. **找美食** - 搜尋特定類型的美食或餐廳
2. **選擇地點** - 搜尋特定地點附近的餐廳

## 主要特性

### 1. 即時搜尋
- 用戶輸入時會自動觸發搜尋（500ms 延遲）
- 支援按 Enter 鍵快速搜尋
- 點擊搜尋按鈕執行搜尋

### 2. 搜尋建議
- 食物類型建議：中式料理、美式漢堡、韓式烤肉等
- 地點建議：台北市、新北市、各區等
- 智能過濾顯示相關建議

### 3. 清空功能
- 每個輸入框都有清空按鈕（×）
- 點擊清空按鈕可快速清除輸入內容
- 清空後自動重新搜尋

### 4. 搜尋結果顯示
- 顯示餐廳卡片包含圖片、名稱、評分、地址、營業狀態
- 結果標題顯示搜尋條件和結果數量
- 支援點擊餐廳卡片查看詳細資訊

## 搜尋邏輯

### 食物關鍵字搜尋
支援以下搜尋方式：
- 餐廳名稱包含關鍵字
- 餐廳類型包含關鍵字
- 預定義食物類型關鍵字匹配

**支援的食物類型：**
- 中式：中餐、中式料理、餃子、麵食、粥、炒飯等
- 美式：漢堡、美式餐廳、炸雞、牛排館等
- 韓式：韓式料理、韓國料理、韓式烤肉等
- 義式：義大利麵、披薩、義式餐廳等
- 法式：法式料理、法國菜、法式甜點等
- 泰式：泰國菜、泰式料理、泰式火鍋等
- 火鍋：麻辣鍋、涮涮鍋、石頭火鍋等
- 牛排：牛排館、排餐、舒肥牛排等
- 燒烤：燒肉、BBQ、串燒、炭烤等
- 飲品：手搖飲、咖啡廳、茶飲等
- 異國料理：印度菜、越南菜、墨西哥菜等

### 地點關鍵字搜尋
支援以下搜尋方式：
- 餐廳地址包含關鍵字
- 格式化地址包含關鍵字
- 附近區域包含關鍵字

## 檔案結構

```
js/
├── searchService.js      # 搜尋服務核心邏輯
├── searchController.js   # 搜尋控制器，處理 UI 互動
└── config.js            # 配置文件

index.html               # 主頁面，包含搜尋欄
test-search.html         # 搜尋功能測試頁面
```

## 使用方法

### 基本搜尋
1. 在「找美食」輸入框中輸入想要的美食類型
2. 在「選擇地點」輸入框中輸入地點
3. 點擊搜尋按鈕或按 Enter 鍵執行搜尋

### 搜尋建議使用
1. 在輸入框中開始輸入
2. 系統會顯示相關的搜尋建議
3. 點擊建議項目快速填入並搜尋

### 清空搜尋
1. 點擊輸入框右側的 × 按鈕清空內容
2. 或直接刪除輸入內容

## 技術實現

### 搜尋服務 (SearchService)
- 負責核心搜尋邏輯
- 支援多種搜尋條件組合
- 整合本地餐廳資料庫
- 支援地理位置獲取

### 搜尋控制器 (SearchController)
- 處理用戶界面互動
- 管理搜尋建議顯示
- 控制搜尋結果展示
- 處理錯誤狀態

### 資料整合
- 從 `restaurantService` 或 `favoriteService` 獲取餐廳資料
- 解析 `json_raw` 欄位中的餐廳類型資訊
- 支援多種資料格式的餐廳資訊

## 錯誤處理

- 網路錯誤：顯示「搜尋失敗，請稍後再試」
- 無結果：顯示「找不到符合條件的餐廳」
- 載入中：顯示載入動畫和提示文字

## 瀏覽器相容性

- 支援現代瀏覽器的地理位置 API
- 支援 ES6+ 語法
- 響應式設計，支援手機和桌面瀏覽

## 測試

可以使用 `test-search.html` 頁面來測試搜尋功能：
1. 開啟 `test-search.html`
2. 輸入搜尋條件
3. 查看搜尋結果

## 未來擴展

- 支援更複雜的搜尋條件（價格範圍、營業時間等）
- 增加搜尋歷史記錄
- 支援語音搜尋
- 增加搜尋結果排序功能 